# Base URL of the CMS (Payload). Used by API routes to fetch assessments.
# Default: http://localhost:3001
# CMS_URL=http://localhost:3001

# Optional. URL of the main site dashboard (e.g. https://your-site.vercel.app/dashboard). When set, a "Back to dashboard" link is shown at the top.
# NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000/dashboard

# Optional. When set, the image-choice app sends this in the x-image-choice-secret header when saving responses; CMS must have the same value. Leave unset for local dev.
# IMAGE_CHOICE_API_SECRET=your-production-secret

# Optional. Must match the site app. When set, the site includes a token in the image-choice link; image-choice verifies it and associates saved responses with that user. Leave unset if not using dashboard links.
# IMAGE_CHOICE_TOKEN_SECRET=your-shared-secret
