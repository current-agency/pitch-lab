# =============================================================================
# Image Choice app – copy to .env and set values
# =============================================================================
# Run: pnpm --filter @repo/image-choice dev (port 3002)
# Start CMS first: pnpm --filter @repo/cms dev
# =============================================================================

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------

# CMS base URL (no trailing slash). Local: http://localhost:3001
CMS_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Optional – shared secret (set SAME value in: site, CMS, audience-poker, stakeholder-map)
# -----------------------------------------------------------------------------
# Needed for: dashboard link token + saving responses to CMS. Leave unset to skip user association.
# ACTIVITY_LINK_SECRET=your-shared-secret

# -----------------------------------------------------------------------------
# Optional – UX
# -----------------------------------------------------------------------------
# "Back to dashboard" link when set.
# NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000/dashboard
