# Base URL of the CMS (Payload). Used by API routes to fetch activities and save submissions.
# Default: http://localhost:3001
# CMS_URL=http://localhost:3001

# Optional. When set, the audience-poker app sends this in the x-audience-poker-secret header when creating submissions; CMS must have the same value. Leave unset for local dev.
# AUDIENCE_POKER_API_SECRET=your-production-secret

# Optional. Must match the site app. When set, the site includes a token in the audience-poker link; submissions are associated with that user. Leave unset if not using dashboard links.
# AUDIENCE_POKER_TOKEN_SECRET=your-shared-secret

# Optional. URL of the main site dashboard. When set, a "Back to dashboard" link is shown.
#NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000/dashboard
