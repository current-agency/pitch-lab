# Base URL of the CMS (Payload). Used by API routes to fetch activities and save submissions.
# Default: http://localhost:3001
# CMS_URL=http://localhost:3001

# Optional. Must match the site and CMS (ACTIVITY_LINK_SECRET). Used for the token in dashboard links and when this app calls the CMS. Set the same value in site, CMS, and this app.
# ACTIVITY_LINK_SECRET=your-shared-secret

# Optional. URL of the main site dashboard. When set, a "Back to dashboard" link is shown.
#NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000/dashboard
