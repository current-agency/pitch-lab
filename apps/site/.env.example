# =============================================================================
# Site (dashboard + login) - copy to .env and set values
# =============================================================================
# Run: pnpm --filter @repo/site dev (port 3000). Start CMS first.
# =============================================================================

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------
# CMS base URL (no trailing slash). Local: http://localhost:3001
CMS_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Optional - activity app URLs (dashboard card links). Defaults: localhost:3002, 3003, 3005, 3007, 3008
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_IMAGE_CHOICE_URL=http://localhost:3002
# NEXT_PUBLIC_CONTENT_RANK_URL=http://localhost:3003
# NEXT_PUBLIC_SURVEY_URL=http://localhost:3005
# NEXT_PUBLIC_AUDIENCE_POKER_URL=http://localhost:3007
# NEXT_PUBLIC_STAKEHOLDER_MAP_URL=http://localhost:3008

# -----------------------------------------------------------------------------
# Optional - shared secret for activity links. Set SAME in: CMS, image-choice, audience-poker, stakeholder-map
# -----------------------------------------------------------------------------
# ACTIVITY_LINK_SECRET=your-shared-secret

# -----------------------------------------------------------------------------
# Optional - Shadcn Blocks: https://www.shadcnblocks.com/dashboard/api
# -----------------------------------------------------------------------------
# SHADCNBLOCKS_API_KEY=your_key_here
