# =============================================================================
# Site app – local development
# =============================================================================
# Copy this file to .env and fill in values.
#
# For login to work locally:
#   1. Start the CMS first: pnpm --filter @repo/cms dev  (runs on port 3001)
#   2. Set CMS_URL below to where the CMS is running (default: http://localhost:3001)
#   3. Start the site: pnpm --filter @repo/site dev
# =============================================================================

# Required for login and dashboard. Must point to the running CMS.
# Local: use the URL/port your CMS runs on (default 3001).
# Production: set in Vercel to your deployed CMS URL (e.g. https://your-cms.vercel.app). No trailing slash.
CMS_URL=http://localhost:3001

# Optional – dashboard app links. Defaults below work if those apps run on the usual ports.
# NEXT_PUBLIC_IMAGE_CHOICE_URL=http://localhost:3002

# Optional. For image-choice: associate saved responses with the logged-in user. Set the SAME value in apps/image-choice/.env (and restart both apps). Works in local and production. Leave unset to save responses without a user.
# IMAGE_CHOICE_TOKEN_SECRET=your-shared-secret
# NEXT_PUBLIC_CONTENT_RANK_URL=http://localhost:3003
# NEXT_PUBLIC_SURVEY_URL=http://localhost:3005
# NEXT_PUBLIC_AUDIENCE_POKER_URL=http://localhost:3007
# NEXT_PUBLIC_STAKEHOLDER_MAP_URL=http://localhost:3008
# Optional. For audience-poker: associate submissions with the logged-in user. Set the SAME value in apps/audience-poker/.env. Leave unset to open activities without a user token.
# AUDIENCE_POKER_TOKEN_SECRET=your-shared-secret

## Shared secret with the site app; used to verify the token in the dashboard link.
STAKEHOLDER_MAP_TOKEN_SECRET=inrneKenrUC07js4GcqlSx0xHyAwDOxbgCvI/

# Shadcn Blocks API key (needed if you use pro blocks like application-shell5).
# Get a key at https://www.shadcnblocks.com/dashboard/api
# SHADCNBLOCKS_API_KEY=your_key_here
