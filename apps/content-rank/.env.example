# =============================================================================
# Content Rank (Content Migration Analyzer) – copy to .env and set values
# =============================================================================
# Run: pnpm --filter @repo/content-rank dev (port 3003)
# Start CMS first: pnpm --filter @repo/cms dev
# =============================================================================

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------

# CMS base URL (no trailing slash). Local: http://localhost:3001
CMS_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Optional – shared secret (set SAME value in: CMS)
# -----------------------------------------------------------------------------
# Used for migration session API (create/update sessions by email). Leave unset if not using that feature.
# MIGRATION_SESSION_API_SECRET=your-migration-secret
