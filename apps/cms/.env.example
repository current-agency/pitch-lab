# =============================================================================
# CMS app – local development
# =============================================================================
# Copy this file to .env and fill in values.
#
# To run the CMS locally:
#   1. Have MongoDB running (local or Atlas). Local default: mongodb://localhost:27017/payload-turbo
#   2. Set PAYLOAD_SECRET (any string; use 32+ chars in production).
#   3. Set DATABASE_URI if not using the default below.
#   4. Start the CMS: pnpm --filter @repo/cms dev  (runs on port 3001)
#
# Production (e.g. Vercel): set PAYLOAD_SECRET, DATABASE_URI, and optionally
# PAYLOAD_PUBLIC_SERVER_URL in the project's Environment Variables. Do not
# put production secrets in .env.
# =============================================================================

# Required. Used to sign cookies/tokens. Use a long random string in production (min 32 chars).
# Generate one: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-key-min-32-chars-long

# MongoDB connection (required for CMS to start). Payload uses DATABASE_URI or MONGODB_URI.
# Option A – Local MongoDB: install and start MongoDB, then use:
DATABASE_URI=mongodb://localhost:27017/payload-turbo
#   macOS (Homebrew): brew tap mongodb/brew && brew install mongodb-community && brew services start mongodb-community
#   Docker: docker run -d -p 27017:27017 --name mongo mongo:7
# Option B – MongoDB Atlas: create a free cluster, get connection string, URL-encode password:
# DATABASE_URI=mongodb+srv://user:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
# Payload also accepts MONGODB_URI if you prefer that name.

# Optional. Production URL (no trailing slash). On Vercel this is often auto-set from VERCEL_URL.
# Set for custom domains or when OG/auth need a fixed URL.
# PAYLOAD_PUBLIC_SERVER_URL=https://your-cms.vercel.app

# Optional. Override where CSV uploads are stored (only if you hit path issues).
# CSV_UPLOAD_DIR=/absolute/path/to/apps/cms/csv-uploads

# Optional. Shared secret for Content Migration Analyzer (content-rank app). Set the same in content-rank's .env.
# MIGRATION_SESSION_API_SECRET=your-shared-secret

# Optional. When set, the survey app must send this in the x-platform-survey-secret header.
# Set the same in the survey app's .env. Leave unset for local dev; set in production.
# PLATFORM_SURVEY_API_SECRET=your-production-secret

# Optional. When set, the image-choice app must send this in the x-image-choice-secret header when saving responses. Set the same in image-choice's .env. Leave unset for local dev.
# IMAGE_CHOICE_API_SECRET=your-production-secret
