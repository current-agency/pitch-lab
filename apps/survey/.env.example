# =============================================================================
# Survey app (Platform Fit Quiz) – local development
# =============================================================================
# Copy this file to .env and fill in values.
#
# To run the survey locally:
#   1. Start the CMS first: pnpm --filter @repo/cms dev  (runs on port 3001)
#   2. Set CMS_URL below to where the CMS is running (default: http://localhost:3001)
#   3. Start the survey: pnpm --filter @repo/survey dev
#   4. Open the survey (e.g. http://localhost:3005). Use ?company=<id> if the site sends one.
#
# Production (e.g. Vercel): set CMS_URL to your deployed CMS URL and optionally
# PLATFORM_SURVEY_API_SECRET (same value as on the CMS) in the project's Environment Variables.
# =============================================================================

# Required. Must point to the running CMS so the survey can load questions and submit responses.
# Local: use the URL/port your CMS runs on (default 3001).
# Production: set in Vercel to your deployed CMS URL (e.g. https://your-cms.vercel.app). No trailing slash.
CMS_URL=http://localhost:3001

# Optional. When set, the survey sends this in the x-platform-survey-secret header; the CMS must have the same value.
# Leave unset for local dev; set in production so only this app can call survey endpoints.
# PLATFORM_SURVEY_API_SECRET=your-production-secret

# Optional. URL of the main site dashboard (e.g. https://your-site.vercel.app/dashboard). When set, a "Back to dashboard" link is shown at the top.
# NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000/dashboard
