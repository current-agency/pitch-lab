# CMS base URL for survey API routes (questions, config, responses).
# Local: http://localhost:3001
# Production: your deployed CMS URL (e.g. https://pitch-lab-cms.vercel.app)
# CMS_URL=http://localhost:3001

# Platform survey API secret: must match PLATFORM_SURVEY_API_SECRET on the CMS in production.
# When the CMS has this set, the survey app sends it in the x-platform-survey-secret header.
# Leave unset for local dev; set in production so the CMS accepts requests from this app.
# PLATFORM_SURVEY_API_SECRET=your-production-secret
