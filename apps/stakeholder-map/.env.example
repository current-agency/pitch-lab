# =============================================================================
# Stakeholder Map - copy to .env and set values
# =============================================================================
# Run: pnpm --filter @repo/stakeholder-map dev (port 3008). Start CMS first.
# =============================================================================

# Required
CMS_URL=http://localhost:3001

# Optional - set SAME in: site, CMS, image-choice, audience-poker
# ACTIVITY_LINK_SECRET=your-shared-secret

# Optional - "Back to dashboard" link
# NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000/dashboard
